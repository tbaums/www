{
  "license": "Apache-2.0",
  "devDependencies": {
    "vuepress": "^1.0.2"
  },
  "scripts": {
    "docs:dev": "vuepress dev content",
    "docs:build": "rm -rf docs/.vuepress/dist/ && rm -rf node_modules && yarn install && vuepress build content",
    "docs:clean": "rm -rf node_modules && yarn install"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "markdown-it-footnote": "^3.0.2",
    "vuepress-plugin-container": "^2.0.2"
  }
}
